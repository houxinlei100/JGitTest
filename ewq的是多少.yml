kind: "VirtualService"
metadata:
  name: "ewq的是多少"
spec:
  hosts:
  - "1.com"
  http:
  - retries:
      attempts: 1
      perTryTimeout: "2s"
    match:
    - headers:
        1:
          regex: "2"
    route:
    - destination:
        host: "2"
        subset: "2"
      weight: "2"
    timeout: "2s"
